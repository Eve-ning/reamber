#ifndef STATUSMESSAGE_H
#define STATUSMESSAGE_H
#include <QString>

namespace STATUSMESSAGE {

    void sendIndex(int messageIndex, QString tag = ""); //Sends message based on index
    void sendCustom(QString tag, int
    void clear();
    
}


#endif // STATUSMESSAGE_H